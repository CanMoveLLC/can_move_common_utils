// Define a callback that takes a boolean parameter.
typedef BooleanCallback = void Function(bool);

// Define a callback that takes a string parameter.
typedef StringCallback = void Function(String?);

// Define a callback that takes a DateTime parameter.
typedef DateTimeCallback = void Function(DateTime);

// Define a callback that takes an integer parameter.
typedef IntCallback = void Function(int);

// Define a callback that takes a dynamic parameter.
typedef DynamicCallback = void Function(dynamic);
